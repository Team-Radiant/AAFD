draw_self()
if (timer >= 500 && timer <= 800)
{
    draw_set_color(c_gray)
    draw_set_font(fnt_1)
    draw_text((room_width / 3), 900, " But that would be the last ")
    draw_text((room_width / 3), 950, "Christmas we'd spend together.")
    image_index = 1
}
